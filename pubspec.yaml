name: reduced_getit
version: 0.4.1
description: Implementation of the state management API 'reduced' on top of the state management framework 'GetIt'.
homepage: https://github.com/partmaster/reduced_getit
repository: https://github.com/partmaster/reduced_getit.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
#  reduced: 0.4.1
  reduced: 
    git:
      url: https://github.com/partmaster/reduced.git
      ref: v0.4.1-beta.4
  get_it: ^7.2.0
  get_it_mixin: ^3.1.4+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
